<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if cart == {} %>
    <%= render 'empty_cart' %>
  <% else %>
    <%= render 'cart_items' %>
  <% end %>

</section>
